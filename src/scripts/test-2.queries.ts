/** Types generated for queries found in "src/scripts/test-2.ts" */

/** Query 'Await' is invalid, so its result is assigned type 'never' */
export type IAwaitResult = never;

/** Query 'Await' is invalid, so its parameters are assigned type 'never' */
export type IAwaitParams = never;

/** Query 'Await' is invalid, so its result is assigned type 'never' */
export type IAwaitResult = never;

/** Query 'Await' is invalid, so its parameters are assigned type 'never' */
export type IAwaitParams = never;

